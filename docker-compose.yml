# Docker Compose for CRISPR Analysis Suite
# Usage: docker-compose up -d

version: '3.8'

services:
  crispr-gui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crispr-analysis-suite
    ports:
      - "8501:8501"
    volumes:
      # Mount local data directory for persistent storage
      - ./data:/app/data
      # Mount output directory
      - ./output:/app/output
      # Mount cache for genome data
      - ./cache:/app/cache
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

# Optional: Add Redis for caching (future enhancement)
#  redis:
#    image: redis:alpine
#    container_name: crispr-redis
#    ports:
#      - "6379:6379"
#    volumes:
#      - redis_data:/data

#volumes:
#  redis_data:
